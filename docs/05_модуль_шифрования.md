# 05. Модуль криптографических алгоритмов

**Папка:** `core/encryption/`

---

## 5.1 Назначение модуля

Модуль инкапсулирует реализацию пяти алгоритмов шифрования за единым интерфейсом. Это позволяет экспериментальному слою вызывать любой алгоритм одинаково, не зная деталей реализации.

**Библиотека:** `pycryptodome`

---

## 5.2 Базовый класс `base_cipher.py`

```python
# core/encryption/base_cipher.py
from abc import ABC, abstractmethod
import time

class BaseCipher(ABC):
    """Абстрактный интерфейс для всех алгоритмов шифрования."""

    @property
    @abstractmethod
    def name(self) -> str:
        """Название алгоритма (для отчётов)."""

    @abstractmethod
    def generate_key(self) -> bytes:
        """Генерация ключа подходящей длины."""

    @abstractmethod
    def encrypt(self, data: bytes, key: bytes) -> bytes:
        """Шифрование. Возвращает шифртекст."""

    @abstractmethod
    def decrypt(self, data: bytes, key: bytes) -> bytes:
        """Дешифрование. Возвращает открытый текст."""

    def encrypt_timed(self, data: bytes, key: bytes) -> tuple[bytes, float]:
        """Шифрование с замером времени. Возвращает (шифртекст, секунды)."""
        start = time.perf_counter()
        ciphertext = self.encrypt(data, key)
        elapsed = time.perf_counter() - start
        return ciphertext, elapsed
```

---

## 5.3 `aes_cipher.py` — AES-256-CBC

```python
# core/encryption/aes_cipher.py
from Crypto.Cipher import AES
from Crypto.Util.Padding import pad, unpad
from Crypto.Random import get_random_bytes
from .base_cipher import BaseCipher

class AESCipher(BaseCipher):
    """AES-256 в режиме CBC с PKCS7-паддингом."""

    KEY_SIZE = 32   # 256 бит
    BLOCK_SIZE = 16 # 128 бит

    @property
    def name(self) -> str:
        return "AES"

    def generate_key(self) -> bytes:
        return get_random_bytes(self.KEY_SIZE)

    def encrypt(self, data: bytes, key: bytes) -> bytes:
        iv = get_random_bytes(self.BLOCK_SIZE)
        cipher = AES.new(key, AES.MODE_CBC, iv)
        ct = cipher.encrypt(pad(data, self.BLOCK_SIZE))
        return iv + ct  # IV + шифртекст

    def decrypt(self, data: bytes, key: bytes) -> bytes:
        iv = data[:self.BLOCK_SIZE]
        ct = data[self.BLOCK_SIZE:]
        cipher = AES.new(key, AES.MODE_CBC, iv)
        return unpad(cipher.decrypt(ct), self.BLOCK_SIZE)
```

**Особенности реализации:**
- IV генерируется случайно при каждом шифровании
- IV передаётся как префикс шифртекста (стандартная практика)
- Длина ключа: 256 бит (максимальная криптостойкость AES)

---

## 5.4 `des_cipher.py` — DES-CBC

```python
# core/encryption/des_cipher.py
from Crypto.Cipher import DES
from Crypto.Util.Padding import pad, unpad
from Crypto.Random import get_random_bytes
from .base_cipher import BaseCipher

class DESCipher(BaseCipher):
    """DES в режиме CBC. Устаревший алгоритм, включён для сравнения."""

    KEY_SIZE = 8    # 56 активных бит (+ 8 бит паритета)
    BLOCK_SIZE = 8

    @property
    def name(self) -> str:
        return "DES"

    def generate_key(self) -> bytes:
        return get_random_bytes(self.KEY_SIZE)

    def encrypt(self, data: bytes, key: bytes) -> bytes:
        iv = get_random_bytes(self.BLOCK_SIZE)
        cipher = DES.new(key, DES.MODE_CBC, iv)
        ct = cipher.encrypt(pad(data, self.BLOCK_SIZE))
        return iv + ct

    def decrypt(self, data: bytes, key: bytes) -> bytes:
        iv = data[:self.BLOCK_SIZE]
        ct = data[self.BLOCK_SIZE:]
        cipher = DES.new(key, DES.MODE_CBC, iv)
        return unpad(cipher.decrypt(ct), self.BLOCK_SIZE)
```

**Примечание:** DES с 56-битным ключом считается криптографически слабым. Ожидается, что по метрикам он покажет низкий результат в части лавинного эффекта по сравнению с AES.

---

## 5.5 `blowfish_cipher.py` — Blowfish-CBC

```python
# core/encryption/blowfish_cipher.py
from Crypto.Cipher import Blowfish
from Crypto.Util.Padding import pad, unpad
from Crypto.Random import get_random_bytes
from .base_cipher import BaseCipher

class BlowfishCipher(BaseCipher):
    """Blowfish в режиме CBC. Ключ 128 бит."""

    KEY_SIZE = 16   # 128 бит (допустимо 4–56 байт)
    BLOCK_SIZE = 8

    @property
    def name(self) -> str:
        return "BLOWFISH"

    def generate_key(self) -> bytes:
        return get_random_bytes(self.KEY_SIZE)

    def encrypt(self, data: bytes, key: bytes) -> bytes:
        iv = get_random_bytes(self.BLOCK_SIZE)
        cipher = Blowfish.new(key, Blowfish.MODE_CBC, iv)
        ct = cipher.encrypt(pad(data, self.BLOCK_SIZE))
        return iv + ct

    def decrypt(self, data: bytes, key: bytes) -> bytes:
        iv = data[:self.BLOCK_SIZE]
        ct = data[self.BLOCK_SIZE:]
        cipher = Blowfish.new(key, Blowfish.MODE_CBC, iv)
        return unpad(cipher.decrypt(ct), self.BLOCK_SIZE)
```

---

## 5.6 `rc4_cipher.py` — RC4 (потоковый)

```python
# core/encryption/rc4_cipher.py
from Crypto.Cipher import ARC4
from Crypto.Random import get_random_bytes
from .base_cipher import BaseCipher

class RC4Cipher(BaseCipher):
    """RC4 — потоковый шифр. Устаревший, включён для сравнения."""

    KEY_SIZE = 16   # 128 бит

    @property
    def name(self) -> str:
        return "RC4"

    def generate_key(self) -> bytes:
        return get_random_bytes(self.KEY_SIZE)

    def encrypt(self, data: bytes, key: bytes) -> bytes:
        # RC4 не использует IV — одна и та же операция для encrypt/decrypt
        cipher = ARC4.new(key)
        return cipher.encrypt(data)

    def decrypt(self, data: bytes, key: bytes) -> bytes:
        cipher = ARC4.new(key)
        return cipher.decrypt(data)
```

**Примечание:** RC4 не использует IV. Это делает его уязвимым при повторном использовании ключа. Ожидается, что по метрике KL-дивергенции он покажет худший результат среди потоковых шифров.

---

## 5.7 `chacha20_cipher.py` — ChaCha20 (потоковый)

```python
# core/encryption/chacha20_cipher.py
from Crypto.Cipher import ChaCha20
from Crypto.Random import get_random_bytes
from .base_cipher import BaseCipher

class ChaCha20Cipher(BaseCipher):
    """ChaCha20 — современный потоковый шифр."""

    KEY_SIZE = 32   # 256 бит
    NONCE_SIZE = 12 # 96 бит (RFC 7539)

    @property
    def name(self) -> str:
        return "CHACHA20"

    def generate_key(self) -> bytes:
        return get_random_bytes(self.KEY_SIZE)

    def encrypt(self, data: bytes, key: bytes) -> bytes:
        nonce = get_random_bytes(self.NONCE_SIZE)
        cipher = ChaCha20.new(key=key, nonce=nonce)
        ct = cipher.encrypt(data)
        return nonce + ct   # nonce + шифртекст

    def decrypt(self, data: bytes, key: bytes) -> bytes:
        nonce = data[:self.NONCE_SIZE]
        ct = data[self.NONCE_SIZE:]
        cipher = ChaCha20.new(key=key, nonce=nonce)
        return cipher.decrypt(ct)
```

---

## 5.8 Фабрика шифров `__init__.py`

```python
# core/encryption/__init__.py
from .aes_cipher import AESCipher
from .des_cipher import DESCipher
from .blowfish_cipher import BlowfishCipher
from .rc4_cipher import RC4Cipher
from .chacha20_cipher import ChaCha20Cipher
from .base_cipher import BaseCipher

CIPHER_REGISTRY = {
    "AES": AESCipher,
    "DES": DESCipher,
    "BLOWFISH": BlowfishCipher,
    "RC4": RC4Cipher,
    "CHACHA20": ChaCha20Cipher,
}

def get_cipher(name: str) -> BaseCipher:
    name = name.upper()
    if name not in CIPHER_REGISTRY:
        raise ValueError(f"Неизвестный алгоритм: {name}. Доступные: {list(CIPHER_REGISTRY)}")
    return CIPHER_REGISTRY[name]()
```

---

## 5.9 Сводная таблица алгоритмов

| Алгоритм | Класс | Тип | Размер ключа | Режим | Размер блока |
|----------|-------|-----|-------------|-------|--------------|
| AES | `AESCipher` | Блочный | 256 бит | CBC | 128 бит |
| DES | `DESCipher` | Блочный | 56 бит | CBC | 64 бит |
| Blowfish | `BlowfishCipher` | Блочный | 128 бит | CBC | 64 бит |
| RC4 | `RC4Cipher` | Потоковый | 128 бит | — | — |
| ChaCha20 | `ChaCha20Cipher` | Потоковый | 256 бит | — | — |

---

## 5.10 Важные детали реализации

1. **Паддинг для блочных шифров:** PKCS7 через `Crypto.Util.Padding`
2. **IV/Nonce:** всегда генерируется случайно, передаётся как префикс шифртекста
3. **Ключи:** генерируются через `get_random_bytes()` (криптографически безопасный PRNG)
4. **Обработка ошибок:** модуль не перехватывает ошибки дешифрования — это задача вызывающего кода
5. **Производительность:** для большого объёма данных (1 MB) замерять время через `encrypt_timed()`

---

## 5.11 Чек-лист реализации

- [ ] Реализовать `base_cipher.py` с абстрактным классом
- [ ] Реализовать `aes_cipher.py`
- [ ] Реализовать `des_cipher.py`
- [ ] Реализовать `blowfish_cipher.py`
- [ ] Реализовать `rc4_cipher.py`
- [ ] Реализовать `chacha20_cipher.py`
- [ ] Реализовать фабрику `get_cipher()` в `__init__.py`
- [ ] Проверить round-trip: `decrypt(encrypt(data, key), key) == data` для каждого шифра
- [ ] Проверить, что шифртексты разных вызовов с одним ключом дают разный результат (за счёт случайного IV)
