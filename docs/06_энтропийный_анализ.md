# 06. Модуль энтропийного анализа

**Папка:** `core/entropy/`

---

## 6.1 Назначение модуля

Ядро исследовательской части. Реализует четыре метрики, основанные на теории информации Шеннона. Все функции принимают `bytes` и возвращают `float`.

**Зависимости:** `numpy`, `scipy`

---

## 6.2 `shannon_entropy.py` — Энтропия Шеннона

### Теория

Энтропия Шеннона — мера непредсказуемости/случайности данных:

```
H(X) = - Σ p(xᵢ) · log₂(p(xᵢ))
```

где `p(xᵢ)` — вероятность появления байта `xᵢ` в потоке.

- Минимум: **0 бит/байт** (все байты одинаковы)
- Максимум: **8 бит/байт** (все 256 байт встречаются равновероятно)

Хорошее шифрование должно давать результат **≥ 7.9 бит/байт**.

### Реализация

```python
# core/entropy/shannon_entropy.py
import numpy as np
from config import ENTROPY_BASE

def calculate_shannon_entropy(data: bytes) -> float:
    """
    Вычисляет энтропию Шеннона для байтовых данных.
    Возвращает значение в битах (при ENTROPY_BASE=2).
    """
    if not data:
        return 0.0

    # Подсчёт частот байтов (0–255)
    counts = np.bincount(np.frombuffer(data, dtype=np.uint8), minlength=256)

    # Вероятности (только ненулевые)
    total = len(data)
    probabilities = counts[counts > 0] / total

    # H = -Σ p·log₂(p)
    entropy = -np.sum(probabilities * np.log2(probabilities))
    return float(entropy)


def calculate_entropy_per_block(data: bytes, block_size: int = 256) -> list[float]:
    """
    Вычисляет энтропию для каждого блока данных.
    Позволяет анализировать однородность шифртекста.
    """
    entropies = []
    for i in range(0, len(data), block_size):
        block = data[i:i + block_size]
        if len(block) >= 16:  # минимальный размер для значимого анализа
            entropies.append(calculate_shannon_entropy(block))
    return entropies


def entropy_score(entropy: float, max_entropy: float = 8.0) -> float:
    """
    Нормализованная оценка [0, 1]. 1.0 = идеальная случайность.
    """
    return entropy / max_entropy
```

---

## 6.3 `conditional_entropy.py` — Условная энтропия

### Теория

Условная энтропия `H(Y|X)` оценивает оставшуюся неопределённость шифртекста Y при знании открытого текста X:

```
H(Y|X) = - Σₓ Σᵧ p(x,y) · log₂(p(y|x))
```

Если алгоритм хороший — `H(Y|X) ≈ H(Y)`, т.е. знание открытого текста не даёт информации о шифртексте.

### Реализация

```python
# core/entropy/conditional_entropy.py
import numpy as np

def calculate_conditional_entropy(
    plaintext: bytes,
    ciphertext: bytes,
    n_bins: int = 16
) -> float:
    """
    Вычисляет условную энтропию H(cipher | plain).
    Данные разбиваются на блоки n_bins для построения совместного распределения.

    Аргументы:
        plaintext:  исходные байты
        ciphertext: зашифрованные байты (той же длины)
        n_bins:     число дискретных корзин для квантизации
    """
    min_len = min(len(plaintext), len(ciphertext))
    if min_len == 0:
        return 0.0

    x = np.frombuffer(plaintext[:min_len], dtype=np.uint8)
    y = np.frombuffer(ciphertext[:min_len], dtype=np.uint8)

    # Квантизация в n_bins корзин
    x_bins = (x * n_bins // 256).astype(int)
    y_bins = (y * n_bins // 256).astype(int)

    # Совместное распределение p(x, y)
    joint_counts = np.zeros((n_bins, n_bins), dtype=np.float64)
    for xi, yi in zip(x_bins, y_bins):
        joint_counts[xi, yi] += 1

    joint_prob = joint_counts / min_len

    # Маргинальное распределение p(x)
    marginal_x = joint_prob.sum(axis=1)

    # H(Y|X) = -Σ p(x,y) * log2(p(y|x))
    h_cond = 0.0
    for i in range(n_bins):
        if marginal_x[i] == 0:
            continue
        for j in range(n_bins):
            if joint_prob[i, j] == 0:
                continue
            p_y_given_x = joint_prob[i, j] / marginal_x[i]
            h_cond -= joint_prob[i, j] * np.log2(p_y_given_x)

    return float(h_cond)
```

---

## 6.4 `kl_divergence.py` — Дивергенция Кульбака–Лейблера

### Теория

KL-дивергенция измеряет отклонение распределения байтов шифртекста P от равномерного распределения Q:

```
D_KL(P || Q) = Σ p(x) · log₂(p(x) / q(x))
```

- `D_KL = 0`: распределение идеально равномерное
- `D_KL > 0`: есть отклонение от равномерности (плохой признак для шифра)

### Реализация

```python
# core/entropy/kl_divergence.py
import numpy as np
from scipy.special import rel_entr

def calculate_kl_divergence(data: bytes) -> float:
    """
    Вычисляет KL-дивергенцию между распределением байтов данных
    и идеальным равномерным распределением (1/256 для каждого байта).
    """
    if not data:
        return 0.0

    counts = np.bincount(np.frombuffer(data, dtype=np.uint8), minlength=256)
    total = len(data)

    # Наблюдаемое распределение P
    p = counts / total

    # Равномерное распределение Q
    q = np.ones(256) / 256

    # Добавляем сглаживание Лапласа, чтобы избежать log(0)
    epsilon = 1e-10
    p_smooth = p + epsilon
    p_smooth /= p_smooth.sum()

    # D_KL(P || Q) через scipy (избегаем log(0))
    kl = np.sum(rel_entr(p_smooth, q))  # rel_entr(p, q) = p * log(p/q)
    return float(kl)


def calculate_kl_divergence_custom(p: np.ndarray, q: np.ndarray) -> float:
    """
    Дивергенция между двумя произвольными распределениями.
    p, q: массивы вероятностей, sum = 1.
    """
    epsilon = 1e-10
    p = p + epsilon
    q = q + epsilon
    p /= p.sum()
    q /= q.sum()
    return float(np.sum(p * np.log2(p / q)))
```

---

## 6.5 `mutual_information.py` — Взаимная информация

### Теория

Взаимная информация I(X;Y) показывает, насколько знание открытого текста X уменьшает неопределённость шифртекста Y:

```
I(X;Y) = H(Y) - H(Y|X)
```

или эквивалентно:

```
I(X;Y) = Σₓ Σᵧ p(x,y) · log₂(p(x,y) / (p(x)·p(y)))
```

- `I(X;Y) = 0`: X и Y полностью независимы → идеальный шифр
- `I(X;Y) > 0`: есть статистическая зависимость → уязвимость

### Реализация

```python
# core/entropy/mutual_information.py
import numpy as np
from .shannon_entropy import calculate_shannon_entropy
from .conditional_entropy import calculate_conditional_entropy

def calculate_mutual_information(plaintext: bytes, ciphertext: bytes) -> float:
    """
    Взаимная информация I(X;Y) = H(Y) - H(Y|X).
    Меньше — лучше (меньше зависимость).
    """
    h_cipher = calculate_shannon_entropy(ciphertext)
    h_cond = calculate_conditional_entropy(plaintext, ciphertext)
    mi = h_cipher - h_cond
    return max(0.0, float(mi))  # MI не может быть отрицательной


def calculate_normalized_mi(plaintext: bytes, ciphertext: bytes) -> float:
    """
    Нормализованная взаимная информация NMI ∈ [0, 1].
    NMI = I(X;Y) / H(Y)
    """
    h_cipher = calculate_shannon_entropy(ciphertext)
    if h_cipher == 0:
        return 0.0
    mi = calculate_mutual_information(plaintext, ciphertext)
    return mi / h_cipher
```

---

## 6.6 Единая точка входа `__init__.py`

```python
# core/entropy/__init__.py
from .shannon_entropy import calculate_shannon_entropy, entropy_score
from .conditional_entropy import calculate_conditional_entropy
from .kl_divergence import calculate_kl_divergence
from .mutual_information import calculate_mutual_information, calculate_normalized_mi

def analyze_all(plaintext: bytes, ciphertext: bytes) -> dict:
    """
    Вычисляет все энтропийные метрики за один вызов.
    Возвращает словарь метрик.
    """
    return {
        "shannon_entropy_plain": calculate_shannon_entropy(plaintext),
        "shannon_entropy_cipher": calculate_shannon_entropy(ciphertext),
        "kl_divergence": calculate_kl_divergence(ciphertext),
        "conditional_entropy": calculate_conditional_entropy(plaintext, ciphertext),
        "mutual_information": calculate_mutual_information(plaintext, ciphertext),
        "entropy_score": entropy_score(calculate_shannon_entropy(ciphertext)),
    }
```

---

## 6.7 Интерпретация результатов

| Метрика | Хорошее шифрование | Плохое шифрование |
|---------|-------------------|------------------|
| Shannon entropy | ≥ 7.9 бит/байт | < 7.0 бит/байт |
| KL-дивергенция | ≈ 0 (< 0.01) | > 0.1 |
| Условная энтропия | ≈ Shannon entropy шифртекста | Значительно ниже |
| Взаимная информация | ≈ 0 | > 0.1 |

---

## 6.8 Чек-лист реализации

- [ ] Реализовать `shannon_entropy.py` (с побайтовым подсчётом через `np.bincount`)
- [ ] Добавить `calculate_entropy_per_block()` для анализа однородности
- [ ] Реализовать `conditional_entropy.py` с квантизацией
- [ ] Реализовать `kl_divergence.py` со сглаживанием Лапласа
- [ ] Реализовать `mutual_information.py`
- [ ] Собрать `analyze_all()` в `__init__.py`
- [ ] Проверить: энтропия случайных байт `os.urandom(10000)` ≈ 7.99
- [ ] Проверить: KL-дивергенция случайных байт ≈ 0
