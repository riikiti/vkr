# 09. Модуль управления экспериментами

**Папка:** `experiments/`

---

## 9.1 Назначение модуля

Оркестрирует весь процесс исследования: генерирует данные, запускает шифрование, вызывает модули анализа и сохраняет результаты. Является связующим звеном между вычислительным ядром и аналитическим слоем.

---

## 9.2 Перечень экспериментов

| № | Имя | Файл | Цель |
|---|-----|------|------|
| 1 | Энтропия исходных данных | `entropy_experiment.py` | Сравнить H(plain) по типам данных |
| 2 | Энтропия шифртекста | `entropy_experiment.py` | Сравнить H(cipher) по алгоритмам |
| 3 | Зависимость энтропии от размера | `entropy_experiment.py` | H(cipher) при разных DATA_SIZES |
| 4 | Тест лавинного эффекта | `avalanche_experiment.py` | Avalanche coefficient по алгоритмам |
| 5 | Анализ распределения байтов | `distribution_experiment.py` | KL, χ², корреляция, моменты |

---

## 9.3 Базовый класс `BaseExperiment`

```python
# experiments/base_experiment.py
from abc import ABC, abstractmethod
import pandas as pd
import time

class BaseExperiment(ABC):
    """Шаблон эксперимента."""

    @property
    @abstractmethod
    def name(self) -> str:
        """Название эксперимента."""

    @abstractmethod
    def run(self) -> pd.DataFrame:
        """
        Выполняет эксперимент и возвращает результаты как DataFrame.
        Колонки: algorithm, data_type, data_size + метрики.
        """

    def run_timed(self) -> tuple[pd.DataFrame, float]:
        """Запуск с замером времени."""
        start = time.perf_counter()
        result = self.run()
        elapsed = time.perf_counter() - start
        print(f"[{self.name}] Завершён за {elapsed:.2f}с")
        return result, elapsed
```

---

## 9.4 `entropy_experiment.py` — Энтропийный эксперимент

```python
# experiments/entropy_experiment.py
import pandas as pd
from config import ALGORITHMS, DATA_SIZES, DATA_TYPES
from core.encryption import get_cipher
from core.entropy import analyze_all
from data_generation import generate_data
from .base_experiment import BaseExperiment

class EntropyExperiment(BaseExperiment):
    """
    Вычисляет энтропийные метрики для всех комбинаций:
    алгоритм × тип данных × размер данных.
    """

    @property
    def name(self) -> str:
        return "EntropyExperiment"

    def run(self) -> pd.DataFrame:
        records = []

        for algo_name in ALGORITHMS:
            cipher = get_cipher(algo_name)
            key = cipher.generate_key()

            for data_type in DATA_TYPES:
                for size in DATA_SIZES:
                    # Генерация данных
                    plaintext = generate_data(data_type, size)

                    # Шифрование
                    ciphertext, enc_time = cipher.encrypt_timed(plaintext, key)

                    # Энтропийный анализ
                    metrics = analyze_all(plaintext, ciphertext)

                    records.append({
                        "algorithm": algo_name,
                        "data_type": data_type,
                        "data_size": size,
                        "encrypt_time_sec": enc_time,
                        **metrics,
                    })

        return pd.DataFrame(records)
```

---

## 9.5 `avalanche_experiment.py` — Эксперимент лавинного эффекта

```python
# experiments/avalanche_experiment.py
import pandas as pd
from config import ALGORITHMS, DATA_SIZES, AVALANCHE_TESTS
from core.encryption import get_cipher
from core.avalanche.avalanche_test import run_avalanche_test
from data_generation.random_generator import generate_random_data
from .base_experiment import BaseExperiment

class AvalancheExperiment(BaseExperiment):

    @property
    def name(self) -> str:
        return "AvalancheExperiment"

    def run(self) -> pd.DataFrame:
        records = []

        for algo_name in ALGORITHMS:
            cipher = get_cipher(algo_name)
            key = cipher.generate_key()

            for size in DATA_SIZES:
                data = generate_random_data(size)
                result = run_avalanche_test(cipher, data, key, n_tests=AVALANCHE_TESTS)

                records.append({
                    "algorithm": algo_name,
                    "data_size": size,
                    "avalanche_mean": result["mean"],
                    "avalanche_std": result["std"],
                    "avalanche_min": result["min_val"],
                    "avalanche_max": result["max_val"],
                    "is_good": result["is_good"],
                })

        return pd.DataFrame(records)
```

---

## 9.6 `distribution_experiment.py` — Эксперимент распределений

```python
# experiments/distribution_experiment.py
import pandas as pd
from config import ALGORITHMS, DATA_SIZES, DATA_TYPES
from core.encryption import get_cipher
from core.statistics import analyze_all_statistics
from data_generation import generate_data
from .base_experiment import BaseExperiment

class DistributionExperiment(BaseExperiment):

    @property
    def name(self) -> str:
        return "DistributionExperiment"

    def run(self) -> pd.DataFrame:
        records = []

        for algo_name in ALGORITHMS:
            cipher = get_cipher(algo_name)
            key = cipher.generate_key()

            for data_type in DATA_TYPES:
                for size in DATA_SIZES:
                    plaintext = generate_data(data_type, size)
                    ciphertext = cipher.encrypt(plaintext, key)
                    stats = analyze_all_statistics(plaintext, ciphertext)

                    records.append({
                        "algorithm": algo_name,
                        "data_type": data_type,
                        "data_size": size,
                        **stats,
                    })

        return pd.DataFrame(records)
```

---

## 9.7 `experiment_runner.py` — Главный оркестратор

```python
# experiments/experiment_runner.py
import os
import pandas as pd
from datetime import datetime
from config import RESULTS_DIR
from .entropy_experiment import EntropyExperiment
from .avalanche_experiment import AvalancheExperiment
from .distribution_experiment import DistributionExperiment


def run_all_experiments(save_results: bool = True) -> dict[str, pd.DataFrame]:
    """
    Запускает все эксперименты последовательно.

    Возвращает словарь {имя_эксперимента: DataFrame}.
    Если save_results=True, сохраняет CSV в data/results/.
    """
    experiments = [
        EntropyExperiment(),
        AvalancheExperiment(),
        DistributionExperiment(),
    ]

    all_results = {}

    for exp in experiments:
        print(f"\n=== Запуск: {exp.name} ===")
        df, elapsed = exp.run_timed()
        all_results[exp.name] = df

        if save_results:
            timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
            filename = f"{exp.name}_{timestamp}.csv"
            path = os.path.join(RESULTS_DIR, filename)
            df.to_csv(path, index=False)
            print(f"  Результаты сохранены: {path}")

    return all_results


def run_single_experiment(experiment_name: str) -> pd.DataFrame:
    """
    Запуск одного эксперимента по имени.
    Используется из интерфейса Streamlit.
    """
    registry = {
        "entropy": EntropyExperiment,
        "avalanche": AvalancheExperiment,
        "distribution": DistributionExperiment,
    }
    if experiment_name not in registry:
        raise ValueError(f"Эксперимент '{experiment_name}' не найден")
    exp = registry[experiment_name]()
    df, _ = exp.run_timed()
    return df
```

---

## 9.8 Схема выходных данных

### EntropyExperiment DataFrame
| Колонка | Тип | Описание |
|---------|-----|---------|
| algorithm | str | Название алгоритма |
| data_type | str | Тип входных данных |
| data_size | int | Размер в байтах |
| encrypt_time_sec | float | Время шифрования |
| shannon_entropy_plain | float | H(открытый текст) |
| shannon_entropy_cipher | float | H(шифртекст) |
| kl_divergence | float | KL от равномерного |
| mutual_information | float | I(X;Y) |
| entropy_score | float | Нормализованная оценка [0,1] |

### AvalancheExperiment DataFrame
| Колонка | Тип | Описание |
|---------|-----|---------|
| algorithm | str | Название алгоритма |
| data_size | int | Размер в байтах |
| avalanche_mean | float | Средний коэффициент |
| avalanche_std | float | Стандартное отклонение |
| is_good | bool | Соответствие критерию |

---

## 9.9 Чек-лист реализации

- [ ] Реализовать `base_experiment.py` с абстрактным классом
- [ ] Реализовать `entropy_experiment.py`
- [ ] Реализовать `avalanche_experiment.py`
- [ ] Реализовать `distribution_experiment.py`
- [ ] Реализовать `experiment_runner.py` с `run_all_experiments()`
- [ ] Добавить `run_single_experiment()` для вызова из Streamlit
- [ ] Добавить обработку ошибок: если один алгоритм упал — продолжить остальные
- [ ] Проверить сохранение CSV с временными метками
- [ ] Убедиться, что DataFrame не пустой после выполнения
